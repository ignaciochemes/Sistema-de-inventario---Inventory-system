<div class="container p-4">
    <div class="row">
        <div class="col-md-4 mx-auto">
            <div class="card">
                <div class="card-body">
                    <form action="/categorias/edit/{{categoria.id}}" method="POST">
                        <div class="form-group">
                            <input type="text" value="{{categoria.titulo}}" class="form-control" name="title" placeholder="Categoria" autofocus>
                        </div>
                        <div class="form-group">
                            <textarea name="descripcion" id="" rows="2" class="form-control" placeholder="Descripcion">{{categoria.descripcion}}</textarea>
                        </div>
                        <div class="form-group">
                            <button class="btn btn-success">
                                Guardar
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>